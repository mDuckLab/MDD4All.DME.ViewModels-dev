# MDD4All.DME.ViewModels

ViewModels for MDD4All.DME, the object graph editor application.

This project holds the app's own view models (Editor, DataManager), grouped by feature area in subfolders. It is specific to MDD4All.DME and is not intended as a general purpose library.
